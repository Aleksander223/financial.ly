# financial.ly-backend

## how to run the app

install mongodb

start the mongodb service with
  ```sudo systemctl start mongodb.service```
  
install required packages with
  ```npm install```

run the app with
  ```npm run start```
